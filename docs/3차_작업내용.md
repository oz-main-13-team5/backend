# 3차 작업 내용

## 완료한 작업
- `feat/pills-api`에서 `PillItem` 등 의존 앱 병합 (`apps/pills/models.py`, `apps/users/…`)
- 북마크·마이페이지 단위 테스트 확장 및 통과  
  (`python3 manage.py test apps.bookmarks.tests apps.mypage.tests`)
- Django 설정에 새 앱 등록 및 기본 DB/SECRET_KEY 환경 대비  
  (`config/settings/base.py`, `config/urls.py`)
- 북마크 삭제 API를 `item_seq` 기반으로 변경해 프런트 입력값을 통일
- 마이페이지 닉네임/비밀번호 수정을 `/me/nickname`, `/me/password`로 분리
- 작업 로그 정리 (`docs/2차_작업내용.md`)

## 남은 일
- Postman으로 실 응답(JSON) 확인 및 캡처 공유
- 프론트와 최종 Request/Response 구조 협의
- 배포 환경용 `.env` 정리 및 마이그레이션 공유
